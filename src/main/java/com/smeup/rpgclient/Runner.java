package com.smeup.rpgclient;

import com.smeup.rpgparser.RunnerKt;

public class Runner {
	/*
	 * Simple way to interpret a program.
	 * The first parameter (args[0]) is the name of the program to call
	 * The other are the parameters to pass to the program
	 */
	public static void main(String[] args) {
		RunnerKt.main(args);
	}
}
